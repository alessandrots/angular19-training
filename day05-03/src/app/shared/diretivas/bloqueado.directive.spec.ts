import { BloqueadoDirective } from './bloqueado.directive';

describe('BloqueadoDirective', () => {
  it('should create an instance', () => {
    const directive = new BloqueadoDirective();
    expect(directive).toBeTruthy();
  });
});
