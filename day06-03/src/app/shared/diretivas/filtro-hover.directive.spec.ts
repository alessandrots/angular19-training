import { FiltroHoverDirective } from './filtro-hover.directive';

describe('FiltroHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FiltroHoverDirective();
    expect(directive).toBeTruthy();
  });
});
